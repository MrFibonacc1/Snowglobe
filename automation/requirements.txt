fastapi>=0.115
uvicorn[standard]>=0.32
jsonschema>=4.23
httpx>=0.27
python-dotenv>=1.0
# Optional — only needed when running real integrations:
# composio  # pip install composio  (Composio SDK; needs COMPOSIO_API_KEY)
